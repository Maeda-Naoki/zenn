volumes:
  node_modules:

services:
  node:
    build:
      context: ./
      args:
        UID: $UID
    volumes:
      - ./:/home/author/Article
      # Data volume mount
      - node_modules:/home/author/Article/node_modules
    tty: true
