volumes:
  node_modules:

services:
  node:
    build:
      context: ./
      args:
        UID: $UID
    container_name: Zenn
    volumes:
      - ./:/home/author/Article
      # Data volume mount
      - node_modules:/home/author/Article/node_modules
    working_dir: /home/author/Article
    tty: true
